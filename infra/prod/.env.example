# 도메인 (A레코드가 이 서버로 연결되어야 함)
PROD_DOMAIN=prod.example.com

# 이미지 태그 (dev, dev-<sha>, prod 등)
IMAGE_TAG=prod

# GHCR 네임스페이스 (기본: github repo owner)
GITHUB_REPOSITORY_OWNER=prometheus-p

# Database (required)
DB_USER=habitcashback
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=habitcashback

# Session secret (required)
SESSION_SECRET=CHANGE_ME_LONG_RANDOM

# CORS (must match your Toss console settings)
ALLOW_ORIGIN=https://habitcashback.apps.tossmini.com

# optional (Toss unlink callback basic auth)
AIT_UNLINK_BASIC_AUTH=user:pass

# public support email shown in /support and docs
SUPPORT_EMAIL=support@example.com
